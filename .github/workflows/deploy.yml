name: Deploy to server

on:
  workflow_run:
    workflows: ["Publish prod image"]
    types: [completed]

permissions:
  contents: read
  packages: read

jobs:
  deploy:
    if: ${{ github.event.workflow_run.conclusion == 'success' }}
    runs-on: ubuntu-latest

    steps:
      - name: Deploy on server (SSH)
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USER }}
          key: ${{ secrets.SSH_KEY }}
          script: |
            set -e
            cd /opt/happyrentals
            echo "${{ secrets.GITHUB_TOKEN }}" | docker login ghcr.io -u ${{ github.actor }} --password-stdin            pwsh -NoProfile -NonInteractive -Command "
              $sha='${{ github.event.workflow_run.head_sha }}'.Substring(0,7);
              $env:IMAGE_TAG='sha-' + $sha;
              Set-Location '/opt/happyrentals';
              docker compose -f docker-compose.prod.yml pull;
              docker compose -f docker-compose.prod.yml up -d
            "


